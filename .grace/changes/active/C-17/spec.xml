<GraceChangeSpec graceVersion='4.0' status='approved'>
<C-17>
  <Name>Markdown hygiene: markdownlint-subset validation + Format document (matrix P0-A/P0-B)</Name>
  <Created>2026-09-21</Created>
  <Priority>high</Priority>
  <Status>approved-for-implementation</Status>

  <Summary>
    The two medium-effort P0 gaps from docs/research/markdown-feature-matrix.md,
    both serving the Mark thesis "your agent writes markdown — Mark keeps it
    readable": P0-A live validation with a markdownlint-style rule subset
    (red squiggles in the WYSIWYG surface), P0-B Format document that
    canonicalizes headings/lists/tables/spacing on demand through the
    existing export/import pipeline (getMarkdown → normalize → setMarkdown
    with a single undoable history entry).
  </Summary>

  <Problem>
    <Description>
      Agent-generated markdown arrives with heading-level jumps
      (MD001), inconsistent heading style (MD003), trailing spaces (MD009),
      duplicate top-level headings/multiple H1 (MD025), missing blank lines
      around lists/headings (MD032), and broken link/image references
      (MD040-class). Mark neither flags nor fixes these; VS Code shows both
      via extensions.
    </Description>
    <RootCause>
      No validation pass exists over muya's block tree, and no
      canonicalizing formatter exists — the renderer can already round-trip
      markdown (ExportMarkdown/ImportMarkdown in muya), but nothing applies
      lint rules or normalizes structure on demand.
    </RootCause>
    <Evidence>
      docs/research/markdown-feature-matrix.md Quality &amp; formatting rows
      (markdownlint ❌, Format document ❌, both marked P0); muya
      contentState exposes getBlocks()/getMarkdown() sufficient for both
      features; no markdownlint/prettier dependency in package.json (and
      Constraint: none will be added — the npm markdownlint packages are
      heavy and pull their own parser; rules run on muya blocks instead).
    </Evidence>
  </Problem>

  <Goals>
    <R-1>
      P0-A validator module (renderer, pure): rule subset MD001 (heading
      increments), MD003 (heading style consistency), MD009 (trailing
      spaces), MD025 (single H1), MD032 (blank lines around blocks),
      MD040-class (fenced code language present). AMENDED at
      implementation: input is the document markdown TEXT (line-oriented —
      headings/fences/lists are line-recognizable; block keys are not
      needed for a panel surface), output [{ rule, line, excerpt, message }].
      Deterministic, unit table-driven per rule.
    </R-1>
    <R-2>
      P0-A surface (AMENDED v1): a problems overlay panel (projectSearch
      pattern — bus-toggled, click-to-jump to the nearest heading),
      recomputed on document change (debounced), no interference with
      editing/undo. AMENDED: the panel is the v1 surface; inline red squiggles inside the muya render tree are a
      follow-up (needs a muya decoration lane; deliberately not v1).
      Toggleable via a store preference (default on; palette command
      toggles it; settings-UI section is a follow-up).
    </R-2>
    <R-3>
      P0-B Format document command (Shift+Alt+F — Cmd+Shift+F is taken by
      find-in-folder; palette entry, no native-menu change in v1):
      normalizes heading cascade (MD001 demotion), list/heading blank-line
      structure (MD032), setext→atx headings (MD003), and trailing
      whitespace (MD009) via a pure text formatter; applied through
      setMarkdown with a composed two-state history stack (pre-format,
      post-format) so ONE undo restores the original; idempotent
      (format twice = format once); never changes semantics — text, links,
      and fenced-code CONTENTS are untouched (table alignment is a
      follow-up; MD025/MD040 are validate-only rules, not auto-fixed).
    </R-3>
    <R-4>
      Both features share the rule/normalizer core so a rule and its fix
      cannot drift apart (the formatter consumes the same rule
      definitions).
    </R-4>
  </Goals>

  <Constraints>
    <Constraint>No new npm runtime dependencies — rules/formatter implemented over muya blocks in-repo.</Constraint>
    <Constraint>Validation is advisory-only: no auto-fix on paste/save in this change (Format document is the explicit fix path).</Constraint>
    <Constraint>Formatter must be idempotent (format twice = format once) and semantics-preserving — pinned by property-style tests over fixture documents.</Constraint>
    <Constraint>Scoped 100% coverage of the new validator/formatter modules; e2e visual gate (squiggle visible on a fixture with a heading jump; Format produces expected canonical text); grace lint zero findings; two sequential reviews.</Constraint>
  </Constraints>

  <NonGoals>
    <NonGoal>Full markdownlint rule parity (config files, inline pragma comments) — subset above only.</NonGoal>
    <NonGoal>Quick-fix lightbulbs per squiggle (follow-up once validation ships).</NonGoal>
    <NonGoal>Source-mode (CodeMirror) lint integration — WYSIWYG surface first.</NonGoal>
  </NonGoals>

  <AcceptanceCriteria>
    <Criterion>Unit: each rule fires/does-not-fire on table fixtures; formatter canonicalizes fixtures idempotently; semantics-preserved fixtures (code blocks, links) unchanged.</Criterion>
    <Criterion>Visual e2e: a document with a heading jump shows the problems panel entry; Format document via menu/bus canonicalizes the visible text; undo restores the original in one step (real-muya pin).</Criterion>
    <Criterion>Preference toggle clears/reenables the problems panel without reload.</Criterion>
    <Criterion>Full suite green; two sequential reviews (correctness; contracts/security) clean.</Criterion>
  </AcceptanceCriteria>

  <AffectedAreas>
    <M-011>editor.vue format handler + problems overlay + commands registry entries + markdownLint preference (registry lives in M-011's src/renderer scope)</M-011>
  </AffectedAreas>

  <VerificationIntent>
    Unit: per-rule table tests + formatter idempotence suite. Visual: new
    Playwright spec (squiggle + format + undo). Governance: V-M-012/M-011
    verification deltas at implementation time; new validator module gets
    its own graph entry.
  </VerificationIntent>

  <Approval>
    <ApprovedBy>user (session 2026-09-22): "действуй дальше" — proceed with C-17 after C-16</ApprovedBy>
    <ApprovalScope>
      Renderer-side validation + formatting; no backend changes.
    </ApprovalScope>
  </Approval>
</C-17>
</GraceChangeSpec>
