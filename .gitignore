# mark-electron/ = Phase-A modernization track (fork with 12 PRs merged, still Electron).
# Separate working copy cloned from xronocode/mark#modernize. See SETUP.md.
mark-electron/

# reborn-mark/ = Phase-B Tauri rewrite (the real "reborn" on new stack).
# Currently empty; scaffolded via `cargo tauri init` during Phase-B1.
# When populated, it will also be a separate working copy (branch `reborn` of xronocode/mark).
reborn-mark/

# Node / Yarn byproducts (in case any sneak in)
node_modules/
.pnpm-store/
dist/
build/

# Tooling
.DS_Store
.vscode/
.idea/
*.log

# Our own transient state
.claude/
