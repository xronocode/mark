# --- Working copies (separate git repos that have their own remotes) ---
# Phase-A modernized Electron product (live remote: github.com/xronocode/mark on branch electron).
mark-electron/
# Phase-B Tauri rewrite (will scaffold via `cargo tauri init` in Phase-B1).
reborn-mark/
# Homebrew tap (live remote: github.com/xronocode/homebrew-mark).
homebrew-mark/
# Pristine upstream snapshot, read-only reference.
marktext/

# --- Node / build byproducts ---
node_modules/
.pnpm-store/
dist/
build/

# --- Tooling ---
.DS_Store
.vscode/
.idea/
*.log

# --- Transient agent state ---
.claude/
