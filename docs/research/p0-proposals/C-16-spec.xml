<GraceChangeSpec graceVersion='4.0' status='proposed'>
<C-16>
  <Name>Agent-output paste hygiene: smart paste + clipboard image paste (matrix P0-C/P0-D)</Name>
  <Created>2026-09-21</Created>
  <Priority>high</Priority>
  <Status>proposed-awaiting-approval</Status>

  <Summary>
    Two small paste-pipeline gaps from docs/research/markdown-feature-matrix.md
    P0 list. P0-D smart paste: pasting a URL over a selection wraps it as
    [text](url); pasting rich HTML converts it to markdown via the already
    bundled turndown + joplin-turndown-plugin-gfm (deps verified in
    package.json). P0-C clipboard image paste: restoring the v1 ability to
    paste a screenshot bitmap from the clipboard into the document as a
    local file + relative link — today only file-path clipboard entries
    work (guessClipboardFilePath reads NSFilenamesPboardType/FileNameW);
    bitmap data needs a backend NSPasteboard image reader via
    core-foundation (same raw-sys lane as C-15 bookmarks).
  </Summary>

  <Problem>
    <Description>
      Agents and users paste URLs, rich HTML, and screenshots into Mark.
      Today: a URL paste over a selection discards the selection; rich HTML
      pastes as flattened text or noisy markup; a screenshot bitmap pastes
      as nothing (only Finder-copied file paths are handled by muya's
      clipboardFilePath option).
    </Description>
    <RootCause>
      src/muya/lib/contentState/pasteCtrl.js handles plain text, markdown
      text, and clipboardFilePath images, but has no URL-on-selection
      branch and no HTML branch; the Tauri port has no clipboard image
      bitmap reader at all (v1 Electron read NSPasteboard image data in the
      main process; that code was not ported).
    </RootCause>
    <Evidence>
      pasteCtrl.js:137-175 (imagePath branch only);
      src/renderer/src/util/clipboard.js guessClipboardFilePath
      (NSFilenamesPboardType/FileNameW — file paths, not bitmaps);
      package.json deps turndown@^7.2.2 + joplin-turndown-plugin-gfm
      present; editor.vue imageAction pipeline (line 615, 1594) already
      accepts a file path and applies move-to-folder/upload behavior.
    </Evidence>
  </Problem>

  <Goals>
    <R-1>
      P0-D URL smart paste: when the clipboard holds a bare http(s)/mailto
      URL and the editor has a non-empty selection, paste replaces the
      selection with [selected text](url). Cursor lands after the link.
      With an empty selection, behavior is unchanged (plain URL text).
    </R-1>
    <R-2>
      P0-D HTML smart paste: when the clipboard holds text/html (and no
      image), convert to markdown with turndown + GFM plugin, sanitize the
      HTML input with the existing dompurify pipeline, and paste as
      markdown. A markdown/code block selection pastes as-is (existing
      behavior preserved).
    </R-2>
    <R-3>
      P0-C clipboard image paste: new backend command mt_clipboard_image
      returning { bytes, format } (png/tiff→png) read from NSPasteboard via
      core-foundation raw-sys (no new heavyweight deps); renderer writes
      bytes to the image folder per preferences and inserts the relative
      link through the existing muya imageAction pipeline (pasteCtrl
      imagePath branch). macOS-first; Linux/Windows return an empty result
      (graceful no-op, same as guessClipboardFilePath today).
    </R-3>
    <R-4>
      No regression to the three existing paste behaviors: plain text,
      markdown source paste (multi-line block handling), and Finder
      file-path image paste — pinned by new unit tests plus existing
      coverage.
    </R-4>
  </Goals>

  <Constraints>
    <Constraint>
      No new npm runtime dependencies (turndown/dompurify already bundled).
      Rust side may use core-foundation only (already a dependency for
      C-15).
    </Constraint>
    <Constraint>
      All clipboard reads happen on explicit user paste (Cmd+V / context
      menu Paste) — no background clipboard polling.
    </Constraint>
    <Constraint>
      Sanitization before turndown (DOMPurify) — clipboard HTML is
      untrusted input; no script/style/event-handler survival.
    </Constraint>
    <Constraint>
      Scoped 100% line+branch coverage of changed pasteCtrl/clipboard code
      paths; e2e visual gates for URL-paste and image-paste happy paths via
      the tauri-shim (clipboard mocked); grace lint zero findings.
    </Constraint>
  </Constraints>

  <NonGoals>
    <NonGoal>Clipboard history UI, paste-preview popups, or paste-and-match-style menu plumbing beyond reusing existing entries.</NonGoal>
    <NonGoal>Linux/Windows clipboard image backends (macOS-only product phase).</NonGoal>
    <NonGoal>Image editing/compression of pasted bitmaps (existing imageAction behavior applies as-is).</NonGoal>
  </NonGoals>

  <AcceptanceCriteria>
    <Criterion>Unit: URL-on-selection → [text](url); URL with empty selection → plain text; HTML → sanitized markdown via turndown; plain/markdown paste paths unchanged.</Criterion>
    <Criterion>Unit + Rust: mt_clipboard_image returns PNG bytes for a bitmap clipboard (CF round-trip test like M-047) and empty on Linux-shaped no-op path; renderer file write + link insertion reuse imageAction.</Criterion>
    <Criterion>E2e (Playwright + tauri-shim, clipboard mocked): pasting a URL over a selection produces a rendered link; pasting HTML produces the markdown equivalent in the WYSIWYG surface.</Criterion>
    <Criterion>Full suite green; two sequential reviews (correctness/races, security/contracts — clipboard HTML is untrusted input) both clean.</Criterion>
  </AcceptanceCriteria>

  <AffectedAreas>
    <M-012>muya pasteCtrl: URL/HTML smart-paste branches (pasteCtrl.js)</M-012>
    <M-011>editor.vue clipboard option wiring for image bytes path</M-011>
    <M-004>new Rust clipboard-image command (core-foundation lane, sibling of M-047)</M-004>
  </AffectedAreas>

  <VerificationIntent>
    Unit: muya pasteCtrl table-driven tests (URL/HTML/plain/markdown/image
    branches); renderer clipboard util tests; Rust CF round-trip for
    NSPasteboard image read. Visual: new Playwright spec extending
    tests/e2e/ with mocked clipboard payloads through the shim. Governance:
    verification deltas for the owning V-M-* anchors at implementation
    time.
  </VerificationIntent>

  <Approval>
    <ApprovedBy>pending user approval (proposed 2026-09-21 from the P0 feature-matrix review)</ApprovedBy>
    <ApprovalScope>
      Paste-pipeline additions only; no editor architecture changes.
    </ApprovalScope>
  </Approval>
</C-16>
</GraceChangeSpec>
